{
  "name": "userhub",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev:api": "pnpm --filter api start:dev",
    "dev:web": "pnpm --filter web dev",
    "dev:mock": "pnpm --filter mock-analytics start:dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "format": "pnpm exec biome check --write"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4"
  }
}
